import {StyleSheet} from 'react-native';
const styles = StyleSheet.create({
  map: {
    width: '100%',
    height: '100%',
  },
  callout: {
    width: 220,
    height: 160, // Alterado para simplificar
    justifyContent: 'center',
    alignItems: 'flex-start',
    backgroundColor: '#f0f0ff',
  },
});

export default styles;
